<div>
  <h1>
    <%= t '.event' %>
    
  </h1>
  <p><%= link_to @event.title, @event %></p>
  <h2>
    <%= t '.title' %>
  </h2>
  <% @photos.each do |photo| %>
    <%= image_tag photo.source.url, style: "width: 300px; height:auto;" %>
  <% end %>
</div>
